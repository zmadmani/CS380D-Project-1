# CS380D-Project-1

COMMAND FORMAT: [ID]:[MESSAGE]

MESSAGES:
	SEND_REQ:[THING TO VOTE ON]
	YES
	NO
	ABORT
	PRECOMMIT
	COMMIT

COMMANDS:
	ADD(songName,URL)
	REMOVE(songName)
	EDIT(songName,newSongName,newSongURL)